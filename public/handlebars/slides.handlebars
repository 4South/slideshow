{{#each slide in arrangedContent}}
  <div class = "slide">
    {{#view contentBinding = "slide"}}
      {{partial presentationslide}}
      <hr>
    {{/view}}
  </div>
  
{{else}}
  
  <div class="lead">No Slides</div>

{{/each}}
