<div class="controls">
  {{#if controller.atleastOneSlide}}
    <button class="btn-small btn-warning" {{action startShow}}>
      Play
    </button>
  {{else}}
    <p class="lead">No Slides</p>
  {{/if}}
</div>
