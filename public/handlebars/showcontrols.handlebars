<div class="row controls">
  <button class="btn btn-warning" {{action pauseShow}}>
    Pause
  </button>
  <button class="btn btn-primary" {{action back}}>
    Back
  </button>
  <button class="btn btn-primary" {{action forward}}>
    Forward
  </button>
</div>
